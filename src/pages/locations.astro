---
// import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';
// import BlogPost from '../components/BlogPost.astro';
// const allPosts = await Astro.glob('../pages/locations/*.astro');
const pageTitle = "Locations";

// console.log(allPosts[0])
// console.log(allPosts[0].url)
// console.log(allPosts[0].file)
---


<BaseLayout pageTitle={pageTitle}>
  <section class="page-header">
    <h1 class="h1-default">{pageTitle}</h1>
  </section>

  <section class="mb-20 text-center">
      <div class="max-w-xl px-5 mx-auto my-0">
        <p>Currently providing home and commercial facility cleaning services for all of San Diego County.</p>
      </div>
  </section>
    <!-- <ul>
      {allPosts.map((post) => <BlogPost url={post.url} title="San diego" />)}
    </ul> -->
</BaseLayout>